{
  "name": "historia",
  "private": true,
  "description": "Personal history notebook — FastAPI + vanilla JS (npm scripts wrap Python)",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "dev": "node scripts/dev.mjs",
    "stop": "node scripts/stop.mjs",
    "restart": "node scripts/restart.mjs",
    "start": ".venv/bin/python run.py"
  },
  "engines": {
    "node": ">=18"
  }
}
