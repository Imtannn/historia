# Copy to .env for local overrides (optional). Railway sets these in the dashboard.

# Persistent data (Railway volume mount at /data)
DATA_DIR=/data
SQLITE_PATH=/data/historia.db

# Railway injects PORT automatically
# PORT=8000
